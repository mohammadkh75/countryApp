import './style.css'

 import { createHeader } from './header';
 import { createSearchAndFilter } from './searchAndFilter';

      document.addEventListener('DOMContentLoaded', () => {
        createHeader();
      });
      

  document.addEventListener('DOMContentLoaded', () => {
    createSearchAndFilter();
  });



